---
import hljs from "highlight.js";
const { node } = Astro.props;
const { code, language } = node;

console.log({language})

const codeData = hljs.highlight(code, { language: language ?? "javascript" });
---

<pre><code set:html={codeData.value} /></pre>
